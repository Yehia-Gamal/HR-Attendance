# Emergency Admin Access Patch

تمت إضافة ملف التصحيح:

`supabase/sql/patches/004_emergency_admin_access.sql`

## ماذا يفعل؟

- ينشئ/يفعل دور `admin` بصلاحية `*`.
- ينشئ أو يصلح مستخدم Supabase Auth للبريد المحدد داخل الملف.
- يؤكد البريد ويعيد ضبط كلمة المرور المؤقتة.
- ينشئ Identity للبريد حتى يعمل تسجيل الدخول بالبريد وكلمة المرور.
- ينشئ أو يربط موظف Admin داخل `employees`.
- ينشئ أو يصلح `profiles` ويربطه بدور الأدمن.
- يلغي إجبار تغيير كلمة المرور مؤقتًا حتى تستطيع الدخول واختبار النظام.

## بيانات الدخول الافتراضية داخل الملف

- Email: `yehia.gamal.idh@gmail.com`
- Temporary password: `ChangeMe_Admin#2026!`

غيّر كلمة المرور داخل SQL قبل التشغيل إن أردت. بعد الدخول، غيّرها فورًا من Supabase Authentication أو من النظام بعد إصلاح صفحة تغيير كلمة المرور.
